import React from 'react';
import { Layout } from '../layout';
import './style.css';
/**
* @author
* @function Home
**/

export const Home = (props) => {
  return(
    <Layout sidebar>
      content
    </Layout>
   )
}